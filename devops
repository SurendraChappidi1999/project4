this is kumar
fggshjgdfjghdgf
changed branching
check poll scm    //This POLL SCM after commit the code then ,after 1min(Crontab) run in jenkins 
Checking Build periodically // This Build is continuosly run the code as you mentioned time in crontab(1 min)
checking Github hook tool // first add webhook in Github >>project settings>>webhook>>add webhook>> public DNS 'URL'
                 This Build, once commit the code in Github, then automatically run the code with in the seconds.
